events {}
http {
	upstream server {
		server managers:5000;
        server managers:80;
        server worker1:5000;
        server worker1:80;
		location / {
			proxy_pass http://server;
		}							    
	}
}