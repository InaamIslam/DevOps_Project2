events {}
http {
	upstream server {
		server manager:5000;
        server manager:80;
        server worker1:5000;
        server worker1:80;
		location / {
			proxy_pass http://server;
		}							    
	}
}